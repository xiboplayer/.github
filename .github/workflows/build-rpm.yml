name: Build RPM (reusable)

on:
  workflow_call:
    inputs:
      package-name:
        required: true
        type: string
        description: 'RPM package name (e.g. xiboplayer-electron)'
      build-command:
        type: string
        default: ''
        description: 'Build command before RPM (e.g. pnpm run build:linux). Empty = skip.'
      rpm-script:
        type: string
        default: 'build-rpm.sh'
        description: 'Path to the RPM build script'
      rpm-spec:
        type: string
        default: ''
        description: 'Path to the RPM spec file (e.g. package.spec). If provided, uses rpmbuild instead of rpm-script.'
      rpm-output-dir:
        type: string
        default: 'dist'
        description: 'Directory where RPM files are output'
      node-version:
        type: string
        default: '24'
      default-version:
        type: string
        default: '0.2.0'
      publish-to-repo:
        type: boolean
        default: true
        description: 'Publish RPMs to package repository (default: true for all builds)'
      use-gh-packages:
        type: boolean
        default: false
        description: 'Bind @xiboplayer scope to GitHub Packages npm and authenticate via NODE_AUTH_TOKEN. Requires the consumer to pass `secrets: inherit` so XIBO_PLAYERS_READ_TOKEN (org-level or repo-level secret) is available. Default false keeps the legacy public-npm fetch path; per-consumer opt-in via this flag.'
      fedora-version:
        type: string
        default: '43'
        description: 'Fedora version for single-version builds (use fedora-matrix for multi-version)'
      fedora-matrix:
        type: string
        default: ''
        description: 'JSON array of Fedora versions (e.g. ["43", "44"]). Overrides fedora-version.'
      arch-matrix:
        type: string
        default: '["x86_64"]'
        description: 'JSON array of architectures to build (e.g. ["x86_64","aarch64"])'
      source-tarball:
        type: string
        default: 'auto'
        description: 'Source tarball mode: auto (detect dist/build/target), full (entire repo), none (skip)'
      gpg-sign:
        type: boolean
        default: false
        description: 'Enable GPG signing of RPM packages'
    secrets:
      RPM_GPG_KEY:
        required: false
        description: 'ASCII-armored GPG private key for RPM signing'
      RPM_GPG_PASSPHRASE:
        required: false
        description: 'Passphrase for the GPG key'
      PAGES_TOKEN:
        required: false
        description: 'PAT with contents:write on xiboplayer.github.io for central repo publishing'
      R2_ACCESS_KEY_ID:
        required: false
      R2_SECRET_ACCESS_KEY:
        required: false
      R2_ENDPOINT:
        required: false

jobs:
  build:
    name: Build RPM (fc${{ matrix.fedora }}, ${{ matrix.arch }})
    runs-on: ${{ matrix.arch == 'aarch64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    container:
      image: fedora:${{ matrix.fedora }}
    strategy:
      matrix:
        fedora: ${{ fromJSON(inputs.fedora-matrix || format('["{0}"]', inputs.fedora-version || '43')) }}
        arch: ${{ fromJSON(inputs.arch-matrix) }}

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Compute Fedora tag
        id: fedora
        run: |
          echo "tag=fc${{ matrix.fedora }}" >> "$GITHUB_OUTPUT"

      - name: Install rpm tools
        run: dnf install -y rpm-build rpmdevtools createrepo_c git tar gzip

      - name: Install build dependencies from spec
        if: inputs.rpm-spec != ''
        run: |
          dnf install -y 'dnf-command(builddep)'
          dnf builddep -y ${{ inputs.rpm-spec }}

      - name: Install Node.js for pnpm-action
        if: inputs.build-command != ''
        run: dnf install -y nodejs

      - uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4
        if: inputs.build-command != ''

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        if: inputs.build-command != ''
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'pnpm'
          # When use-gh-packages: true, bind @xiboplayer scope to the
          # private GitHub Packages registry. Otherwise empty strings
          # leave setup-node's default behaviour (no scope override).
          # See use-gh-packages input below.
          registry-url: ${{ inputs.use-gh-packages && 'https://npm.pkg.github.com' || '' }}
          scope: ${{ inputs.use-gh-packages && '@xiboplayer' || '' }}

      - name: Install dependencies
        if: inputs.build-command != ''
        env:
          # When use-gh-packages: true, expose the consumer-supplied
          # XIBO_PLAYERS_READ_TOKEN secret to pnpm's npm-protocol auth.
          # Empty string when opted out — the registry-url binding is
          # also empty in that case so this is a no-op.
          NODE_AUTH_TOKEN: ${{ inputs.use-gh-packages && secrets.XIBO_PLAYERS_READ_TOKEN || '' }}
        run: pnpm install --no-frozen-lockfile

      - name: Build application
        if: inputs.build-command != ''
        run: ${{ inputs.build-command }}

      - name: Determine version and release
        id: version
        run: |
          # Strict priority — never fall back to a default. Fail loudly if no source.
          # Priority: git tag > RPM spec > package.json
          VERSION=""
          RELEASE=""

          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            TAG="${{ github.ref_name }}"
            TAG="${TAG#v}"
            if [[ "$TAG" == *-* ]]; then
              VERSION="${TAG%%-*}"
              RELEASE="${TAG#*-}"
            else
              VERSION="$TAG"
              RELEASE="1"
            fi
            echo "Read from git tag: $VERSION-$RELEASE"
          else
            SPEC="${{ inputs.rpm-spec }}"
            if [ -n "$SPEC" ] && [ -f "$SPEC" ]; then
              SPEC_VER=$(grep -m1 '^Version:' "$SPEC" | awk '{print $2}')
              # Skip macros like %{_version}
              if [ -n "$SPEC_VER" ] && [[ "$SPEC_VER" != *"%"* ]]; then
                VERSION="$SPEC_VER"
                RELEASE=$(grep -m1 '^Release:' "$SPEC" | awk '{print $2}' | sed 's/%.*//')
                [ -z "$RELEASE" ] && RELEASE="1"
                echo "Read from RPM spec: $VERSION-$RELEASE"
              fi
            fi

            # Fallback to package.json (for Node projects with macro spec)
            if [ -z "$VERSION" ] && [ -f "package.json" ]; then
              VERSION=$(jq -r '.version' package.json 2>/dev/null)
              [ "$VERSION" = "null" ] && VERSION=""
              if [ -n "$VERSION" ]; then
                RELEASE="1"
                echo "Read from package.json: $VERSION"
              fi
            fi
          fi

          if [ -z "$VERSION" ]; then
            echo "::error::Cannot determine version. Need a git tag, an RPM spec with literal Version:, or a package.json."
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "release=$RELEASE" >> "$GITHUB_OUTPUT"
          echo "Final version: $VERSION-$RELEASE"

      - name: Fetch shared packaging library
        run: |
          curl -fsSL https://raw.githubusercontent.com/xibo-players/.github/main/scripts/packaging/lib-rpm.sh \
            -o /tmp/lib-rpm.sh
          echo "PKG_LIB_RPM=/tmp/lib-rpm.sh" >> "$GITHUB_ENV"

      - name: Build RPM
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          RELEASE="${{ steps.version.outputs.release }}"

          # Priority: script > spec > error
          # When both rpm-script and rpm-spec are provided, the script builds
          # and the spec is only used for dnf builddep (handled in earlier step)
          if [ -f "${{ inputs.rpm-script }}" ]; then
            echo "Building RPM using script: ${{ inputs.rpm-script }}"
            chmod +x ${{ inputs.rpm-script }}
            ./${{ inputs.rpm-script }} "${VERSION}" "${RELEASE}"

          elif [ -n "${{ inputs.rpm-spec }}" ] && [ -f "${{ inputs.rpm-spec }}" ]; then
            echo "Building RPM from spec file: ${{ inputs.rpm-spec }}"

            # Setup rpmbuild directory structure
            rpmdev-setuptree

            # Copy spec file to SPECS directory
            cp "${{ inputs.rpm-spec }}" ~/rpmbuild/SPECS/
            SPEC_FILE=~/rpmbuild/SPECS/$(basename "${{ inputs.rpm-spec }}")

            # Replace version and release in spec file
            sed -i "s/^Version:.*/Version: ${VERSION}/" "$SPEC_FILE"
            sed -i "s/^Release:.*/Release: ${RELEASE}%{?dist}/" "$SPEC_FILE"

            # Create source tarball based on mode
            PACKAGE_NAME="${{ inputs.package-name }}"
            case "${{ inputs.source-tarball }}" in
              full)
                echo "Creating full-source tarball (entire repo)..."
                tar -czf ~/rpmbuild/SOURCES/${PACKAGE_NAME}-${VERSION}.tar.gz \
                  --transform "s,^\.,${PACKAGE_NAME}-${VERSION}," \
                  --exclude='.git' .
                ;;
              none)
                echo "Skipping source tarball creation"
                ;;
              auto|*)
                if [ -d "dist" ]; then
                  echo "Copying dist/ directory to SOURCES..."
                  tar -czf ~/rpmbuild/SOURCES/${PACKAGE_NAME}-${VERSION}.tar.gz dist/
                elif [ -d "build" ]; then
                  echo "Copying build/ directory to SOURCES..."
                  tar -czf ~/rpmbuild/SOURCES/${PACKAGE_NAME}-${VERSION}.tar.gz build/
                elif [ -d "target" ]; then
                  echo "Copying target/ directory to SOURCES..."
                  tar -czf ~/rpmbuild/SOURCES/${PACKAGE_NAME}-${VERSION}.tar.gz target/
                else
                  echo "Warning: No dist/, build/, or target/ directory found for source tarball"
                  echo "If your spec file requires Source0, the build may fail"
                fi
                ;;
            esac

            # Build binary RPM + SRPM
            rpmbuild -ba "$SPEC_FILE"

            # Copy built RPMs and SRPMs to output directory
            mkdir -p ${{ inputs.rpm-output-dir }}
            find ~/rpmbuild/RPMS/ -name "*.rpm" -exec cp {} ${{ inputs.rpm-output-dir }}/ \;
            find ~/rpmbuild/SRPMS/ -name "*.src.rpm" -exec cp {} ${{ inputs.rpm-output-dir }}/ \;

            echo "RPM build completed from spec file"

          else
            echo "Error: Neither RPM script (${{ inputs.rpm-script }}) nor RPM spec file (${{ inputs.rpm-spec }}) found"
            exit 1
          fi

          # Verify RPMs were created
          shopt -s nullglob
          rpm_files=(${{ inputs.rpm-output-dir }}/*.rpm)
          if [ ${#rpm_files[@]} -eq 0 ]; then
            echo "Error: No RPM files found in ${{ inputs.rpm-output-dir }}"
            exit 1
          fi

          echo "Built RPMs:"
          ls -lh ${{ inputs.rpm-output-dir }}/*.rpm

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ${{ inputs.package-name }}-rpm-${{ steps.fedora.outputs.tag }}-${{ matrix.arch }}
          path: ${{ inputs.rpm-output-dir }}/*.rpm
          retention-days: 30

  sign:
    name: Sign RPMs
    needs: build
    runs-on: ubuntu-latest
    container:
      image: fedora:${{ inputs.fedora-version || '43' }}

    steps:
      - name: Download all RPM artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          pattern: ${{ inputs.package-name }}-rpm-*
          merge-multiple: true
          path: rpms/

      - name: List downloaded RPMs
        run: ls -lh rpms/

      - name: Verify RPM integrity
        run: |
          dnf install -y rpm-build
          for rpm_file in rpms/*.rpm; do
            echo "Checking: $(basename $rpm_file)"
            rpm -Kv "$rpm_file" 2>&1 | grep -E "(SHA|digest)" || true
          done

      - name: Import GPG key and sign RPMs
        if: inputs.gpg-sign
        env:
          GPG_KEY: ${{ secrets.RPM_GPG_KEY }}
          GPG_PASSPHRASE: ${{ secrets.RPM_GPG_PASSPHRASE }}
        shell: bash
        run: |
          dnf install -y rpm-sign gnupg2 pinentry

          # Import GPG private key. Secret stores raw ASCII armor
          # (output of `gpg --armor --export-secret-keys`) — pipe directly
          # to gpg, no base64 layer.
          printf '%s\n' "$GPG_KEY" | gpg --batch --import

          # Derive fingerprint from the imported key — single source of
          # truth, no separate RPM_GPG_KEY_ID secret that can drift.
          GPG_KEY_ID=$(gpg --list-secret-keys --with-colons \
            | awk -F: '/^fpr:/ {print $10; exit}')
          if [ -z "$GPG_KEY_ID" ]; then
            echo "::error::No secret key imported from RPM_GPG_KEY"
            exit 1
          fi
          echo "Signing with key $GPG_KEY_ID"

          # Create a custom pinentry that serves the passphrase non-interactively.
          # RPM 6.0 (Fedora 43) ignores %__gpg_sign_cmd macro overrides and
          # invokes gpg which starts gpg-agent which calls pinentry. In a CI
          # container there's no /dev/tty, so we replace pinentry with a script.
          printf '%s' "$GPG_PASSPHRASE" > /tmp/.gpg-passphrase
          chmod 600 /tmp/.gpg-passphrase
          cat > /usr/local/bin/pinentry-ci << 'PINENTRY'
          #!/bin/bash
          echo "OK Pleased to meet you"
          while IFS= read -r cmd; do
            case "${cmd%%\ *}" in
              GETPIN)
                echo "D $(cat /tmp/.gpg-passphrase)"
                echo "OK"
                ;;
              BYE)
                echo "OK closing connection"
                exit 0
                ;;
              *)
                echo "OK"
                ;;
            esac
          done
          PINENTRY
          chmod 755 /usr/local/bin/pinentry-ci

          # Configure gpg-agent to use our custom pinentry
          mkdir -p ~/.gnupg
          chmod 700 ~/.gnupg
          echo "pinentry-program /usr/local/bin/pinentry-ci" >> ~/.gnupg/gpg-agent.conf
          gpgconf --kill gpg-agent || true

          echo "%_gpg_name ${GPG_KEY_ID}" >> ~/.rpmmacros

          # Sign all RPMs (binary RPMs must succeed; SRPMs may have
          # corrupt payload checksums for large packages, so warn only)
          sign_failed=0
          for rpm_file in rpms/*.rpm; do
            echo "Signing: $(basename $rpm_file)"
            if rpm --addsign "$rpm_file"; then
              echo "  Signed successfully"
            elif [[ "$rpm_file" == *.src.rpm ]]; then
              echo "  WARNING: Failed to sign SRPM (corrupt payload checksum?), skipping"
            else
              echo "  ERROR: Failed to sign binary RPM"
              sign_failed=1
            fi
          done
          if [ "$sign_failed" -eq 1 ]; then
            echo "ERROR: One or more binary RPMs failed to sign"
            exit 1
          fi

          # Clean up passphrase
          rm -f /tmp/.gpg-passphrase

          # Verify signatures
          echo "Verifying signatures..."
          rpm -qpi rpms/*.rpm | grep -E "^(Name|Signature)" || true

          # Export public key for repository consumers
          gpg --export --armor "$GPG_KEY_ID" > rpms/RPM-GPG-KEY-${{ inputs.package-name }}

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ${{ inputs.package-name }}-rpm-signed
          path: rpms/
          retention-days: 30

  upload:
    name: Upload to R2
    needs: sign
    # Gate the upload, not just the notify below: an ungated upload
    # overwrites already-published packages with a fresh rebuild on
    # every branch push. RPMs are not reproducible, so the checksum
    # changes while the repository metadata still lists the old one,
    # and dnf then fails with "checksum doesn't match" for everyone.
    if: inputs.publish-to-repo
    runs-on: ubuntu-latest
    container:
      image: fedora:43

    steps:
      - name: Install tools
        run: dnf install -y awscli2

      - name: Download signed RPMs
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: ${{ inputs.package-name }}-rpm-signed
          path: rpms/

      - name: Upload RPMs to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          PACKAGE: ${{ inputs.package-name }}
        run: |
          BUCKET="${R2_BUCKET}"
          FEDORA_VERS="43 44"

          for file in rpms/*.rpm; do
            NAME=$(basename "$file")
            [[ "$NAME" == RPM-GPG-KEY-* ]] && continue

            # Filenames are standard RPM names emitted by rpmbuild, e.g.
            #   xiboplayer-electron-0.7.22-1.fc43.x86_64.rpm
            #   xiboplayer-electron-0.7.22-1.fc44.aarch64.rpm
            # Parse the Fedora version from the %{dist} segment (.fcNN.).
            fc_ver=""
            for v in $FEDORA_VERS; do
              if [[ "$NAME" == *".fc${v}."* ]]; then
                fc_ver="$v"
                break
              fi
            done
            if [ -z "$fc_ver" ]; then
              echo "  SKIP: cannot determine Fedora version from $NAME"
              continue
            fi

            # Determine arch from filename
            if [[ "$NAME" == *.src.rpm ]]; then
              arch="SRPMS"
            elif [[ "$NAME" == *.x86_64.rpm ]]; then
              arch="x86_64"
            elif [[ "$NAME" == *.aarch64.rpm ]]; then
              arch="aarch64"
            elif [[ "$NAME" == *.noarch.rpm ]]; then
              arch="noarch"
            else
              echo "  SKIP unknown arch: $NAME"
              continue
            fi

            # Upload to final repo path
            DEST="$BUCKET/rpm/fedora/$fc_ver/$arch/$NAME"
            echo "  $NAME → rpm/fedora/$fc_ver/$arch/"
            aws s3 cp "$file" "$DEST" --endpoint-url "$R2_ENDPOINT"

            # noarch RPMs also go to x86_64 and aarch64
            if [ "$arch" = "noarch" ]; then
              for extra in x86_64 aarch64; do
                aws s3 cp "$file" "$BUCKET/rpm/fedora/$fc_ver/$extra/$NAME" --endpoint-url "$R2_ENDPOINT"
              done
            fi
          done

  notify:
    name: Trigger repository update
    needs: upload
    if: inputs.publish-to-repo
    runs-on: ubuntu-latest

    steps:
      - name: Trigger update-repos on central pages repo
        env:
          GH_TOKEN: ${{ secrets.PAGES_TOKEN }}
        run: |
          # `curl -sf` alone exits 22 with no output, so a dead PAGES_TOKEN
          # looks identical to a network blip. Report the status and say what
          # to do: packages are already on R2 at this point, they are just not
          # indexed, so the repository serves stale metadata until this runs.
          status=$(curl -s -o /tmp/dispatch-body.txt -w '%{http_code}' -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/xiboplayer/xiboplayer.github.io/dispatches" \
            -d '{"event_type": "update-repos", "client_payload": {"package": "${{ inputs.package-name }}", "repo": "${{ github.repository }}"}}')

          if [ "$status" = "204" ]; then
            echo "Triggered update-repos for ${{ inputs.package-name }}"
            exit 0
          fi

          echo "::error::update-repos dispatch returned HTTP $status: $(cat /tmp/dispatch-body.txt)"
          if [ "$status" = "401" ] || [ "$status" = "403" ] || [ "$status" = "404" ]; then
            echo "The PAGES_TOKEN org secret is expired or lacks contents:write on xiboplayer/xiboplayer.github.io."
          fi
          echo "Packages uploaded to R2 but NOT indexed. Recover with:"
          echo "  gh workflow run update-repos.yml -R xiboplayer/xiboplayer.github.io"
          exit 1
